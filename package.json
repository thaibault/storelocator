{
    "name": "jQuery-storeLocator",
    "version": "1.0.0",
    "description": "serves a google maps store locator",
    "keywords": [
        "jquery", "storeLocator", "store", "locator", "map", "location",
        "finder"
    ],
    "files": ["index.js"],
    "repository": {
        "type": "git",
        "url": "https://github.com/thaibault/jQuery-storeLocator"
    },
    "homepage": "http://torben.website/jQuery-storeLocator",
    "main": "index.compiled.js",
    "scripts": {
        "build": "webOptimizer build",
        "buildDLL": "webOptimizer buildDLL",
        "clear": "webOptimizer clear",
        "document": "webOptimizer document",
        "export": "webOptimizer build '{exportFormat:{external:\"var\",self:\"var\"}}'",
        "lint": "webOptimizer typeCheck && webOptimizer lint",
        "serve": "webOptimizer serve",
        "test": "webOptimizer test",
        "testInBrowser": "webOptimizer testInBrowser",
        "preinstall": "webOptimizer preinstall",
        "postinstall": "webOptimizer build",
        "watch": "webOptimizer build --watch",
        "watchDLL": "webOptimizer buildDLL --watch"
    },
    "license": "CC-BY-3.0",
    "webOptimizer": {
        "development": {"openBrowser": {"url": {
            "__evaluate__": "((sourceFilePath) => `${self.development.server.protocol}://${self.development.server.host}:${self.development.server.port}/${path.basename(sourceFilePath.substring(sourceFilePath.lastIndexOf('!') + 1)).replace(/\\.[^.]+$/, '.html')}${2 < self.givenCommandLineArguments.length && self.givenCommandLineArguments[2] === 'serve' ? '' : '?notrycatch'}`)(self.files.defaultHTML.template)"
        }}},
        "injection": {
            "externalAliases": {
                "googleMarkerClusterer": "MarkerClusterer",
                "jquery": "jQuery",
                "jQuery-tools": "$.Tools"
            },
            "internal": {
                "vendor": {
                    "__evaluate__": "self.contextType === 'main' && 2 < self.givenCommandLineArguments.length && ['buildDLL', 'serve', 'testInBrowser', 'watchDLL'].includes(self.givenCommandLineArguments[2]) ? ['jquery', 'jQuery-tools', 'exports?Class=MarkerClusterer,google=google!imports?google=>{}!googleMarkerClusterer'].concat(self.givenCommandLineArguments[2] === 'serve' ? [] : ['qunitjs', 'webOptimizer/browserAPI']) : []"
                },
                "index": {
                    "__evaluate__": "2 < self.givenCommandLineArguments.length && self.givenCommandLineArguments[2] === 'build' ? '__auto__' : self.contextType === 'main' ? (self.debug && self.dllManifestFilePaths.length ? [`webpack-dev-server/client?${self.development.server.protocol}://0.0.0.0:${self.development.server.port}`, 'webpack/hot/dev-server'] : []).concat(2 < self.givenCommandLineArguments.length && self.givenCommandLineArguments[2] === 'serve' ? 'index' : 'test') : []"
                }
            }
        },
        "module": {"aliases": {
            "googleMarkerClusterer$": "js-marker-clusterer"
        }}
    },
    "dependencies": {
        "webOptimizer": "git+ssh://git@github.com/thaibault/webOptimizer.git",
        "jQuery-tools": "git+ssh://git@github.com/thaibault/jQuery-tools.git",
        "js-marker-clusterer": ">=1.0.0"
    },
    "devDependencies": {
        "canvas": ">=1.4.0",
        "jsdom": ">=9.4.1",
        "qunitjs": ">=2.0.0",
        "qunit-cli": ">=0.2.0*",
        "source-map-support": ">=0.4.2"
    }
}
